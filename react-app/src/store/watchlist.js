



const initialState = {
  subscribed: {}
};

const watchlistReducer = (state = initialState, action) => {
  const newState = { ...state }
  switch (action.type) {
      default:
          return state;
  };
};

export default watchlistReducer;
